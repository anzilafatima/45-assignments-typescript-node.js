// question no # 14 
// foreach

// define array of guest 

let guestList : string [] =  ["javaid","shazia","anzila","emaan"]; 

// invite each guest for dinner 

guestList.forEach(guest => {
     console.log(`Dear ${guest}, you are cordially invited to dinner`)
});
